apiVersion: appmesh.k8s.aws/v1beta2
kind: VirtualService
metadata:
  name: {{ template "noths-deploy.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "noths-deploy.name" . }}
    chart: {{ template "noths-deploy.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  awsName: {{ template "noths-deploy.fullname" . }}.{{ .Values.deployment.dnsDomain }}
  provider:
    virtualRouter:
      virtualRouterRef:
        name: {{ template "noths-deploy.fullname" . }}
